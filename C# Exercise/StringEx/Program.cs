using System;

namespace StringEx
{
    internal class Program
    {
        private static void Main(string[] args)
        {
            /*
            var fullName = "kaleab cherinet ";

            //* trim() -  remove white space
            Console.WriteLine("trim '{0}'", fullName.Trim());

            //* ToUpper() - to uppercase
            Console.WriteLine("To upper '{0}'", fullName.ToUpper());

            //* length
            Console.WriteLine("length: {0}", fullName.Length);

            //* indexOf() - index
            var index = fullName.IndexOf(' ');
            var firstName = fullName.Substring(0, index);
            var lastName = fullName.Substring(++index);
            Console.WriteLine("first name: " + firstName);
            Console.WriteLine("last name: " + lastName);

            //*split() - split them into different arrays
            var names = fullName.Split(' ');
            Console.WriteLine("first name: " + names[0]);
            Console.WriteLine("last name: " + names[1]);

            //* Replace() -  replace anything with given input
            var repSpaceName = fullName.Replace(' ', '/');
            Console.WriteLine(repSpaceName);
            var repCharName = fullName.Replace('a', 'z');
            Console.WriteLine(repCharName);
            var repStringName = fullName.Replace("kaleab", "z");
            Console.WriteLine(repStringName);

            //* IsNullOrWhiteSpace()
            if (string.IsNullOrEmpty(" ")) ;
            Console.WriteLine("invalid");
            */
        }
    }
}